apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpbin
  namespace: httpbin
spec:
  parentRefs:
    - name: http
      namespace: gloo-gateway-system
  hostnames:
    - "www.example2.com"
  rules:
    - backendRefs:
        - name: httpbin
          port: 8000
